{
  "kind": "collectionType",
  "collectionName": "faqs",
  "info": {
    "singularName": "faq",
    "pluralName": "faqs",
    "displayName": "FAQ",
    "description": "Preguntas frecuentes"
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "pregunta": {
      "type": "string",
      "required": true
    },
    "respuesta": {
      "type": "richtext",
      "required": true
    },
    "categoria": {
      "type": "enumeration",
      "enum": ["General", "Membresías", "Capacitaciones", "Materiales", "Reservas", "Equipos"],
      "default": "General",
      "required": true
    },
    "orden": {
      "type": "integer",
      "default": 0
    }
  }
}
